name: 'actions/badge'
description: 'Building badge project'

branding:
  icon: 'box'
  color: 'green'

runs:
  using: "composite"
  steps:

    - if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
      uses: yadickson/actions-steps/project-badge@v1
